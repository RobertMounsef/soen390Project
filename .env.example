# Google Maps API key (directions, map tiles).
# Copy this file to .env and replace with your key. Do not commit .env.
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Google OAuth 2.0 Client ID (Web application) for Calendar API.
# Create in Google Cloud Console: APIs & Services > Credentials > OAuth 2.0 Client ID (Web application).
# Shared repo: use the SAME Client ID for everyone. In Google Console, add one Authorized redirect URI
# per tester: https://auth.expo.io/@THEIR_EXPO_USERNAME/campus-guide (each person's Expo username).
EXPO_PUBLIC_GOOGLE_OAUTH_CLIENT_ID=your_google_oauth_client_id_here

# Expo Auth proxy redirect (so Google sees https://auth.expo.io/... instead of exp://).
# Set to your Expo project full name: @YOUR_EXPO_USERNAME/campus-guide (same as in redirect URI above).
# Each developer sets this in their own .env; do not commit real usernames in .env.example.
EXPO_PUBLIC_EXPO_PROJECT_FULLNAME=@your-expo-username/campus-guide

# Required for Web application OAuth client: Google requires client_secret when exchanging the code for tokens.
# In Google Cloud Console: your OAuth 2.0 Client > copy "Client secret". Put it only in .env (never commit).
# Note: This ends up in the app bundle; for production use a backend to do the token exchange instead.
EXPO_PUBLIC_GOOGLE_OAUTH_CLIENT_SECRET=your_client_secret_here